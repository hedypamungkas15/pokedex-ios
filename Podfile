# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pokedex-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pokedex-ios
  pod 'RxSwift', '6.1.0'
  pod 'RxCocoa', '6.1.0'
  pod 'Kingfisher', '~> 6.0'
  pod 'XLPagerTabStrip', '~> 9.0'
  pod 'JGProgressHUD'

  target 'pokedex-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pokedex-iosUITests' do
    # Pods for testing
  end

end
